name: Discord Heex Server

on:
    [
        pull_request,
        issues,
        issue_comment,
        discussion,
        discussion_comment,
        deployment,
        deployment_status,
    ]

env:
    NODE_ENV: production

jobs:
    notify:
        runs-on: ubuntu-latest

        steps:
            - uses: sarisia/actions-status-discord@v1
              if: always()
              with:
                  webhook: ${{ secrets.DISCORD_HEEX_WEBHOOK }}
